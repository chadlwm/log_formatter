require 'spec_helper'

describe RubyLogFormatter do
  it 'has a version number' do
    expect(RubyLogFormatter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
